"""FastAPI backend for the unmodified legal_graphrag pipeline (agents/legal_pipeline.py, ingestion/pdf_pipeline.py).

No pipeline files are imported-and-changed by this package — it only calls
existing public functions (build_legal_agent_graph, run_pipeline) exactly
as scripts/run_demo.py does.
"""
